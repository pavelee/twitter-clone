import { IconType } from "react-icons";

type SidebarItemProps = {
    label: string;
    href: string;
    icon: IconType;
}

export const SidebarItem = () => {
    return (
        <div></div>
    )
}